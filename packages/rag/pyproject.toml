[project]
name = "urbanpulse-rag"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "chromadb>=0.6.0",
    "httpx>=0.27.0",
]

[tool.uv.sources]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rag"]
