[project]
name = "batch"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "urbanpulse-core @ {root:uri}/../../packages/core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/batch"]
